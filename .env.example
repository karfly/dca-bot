# OKX API credentials
OKX_API_KEY=your_api_key_here
OKX_API_SECRET=your_api_secret_here
OKX_API_PASSPHRASE=your_api_passphrase_here
OKX_SUBACCOUNT_NAME=dca_bot

# Exchange selection
EXCHANGE_ID=okx  # Currently only OKX is fully tested, other CCXT exchanges may work but are untested

# Telegram configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_USER_ID=your_user_id_here
TELEGRAM_NOTIFICATION_SOUND=true

# DCA configuration
DCA_AMOUNT_USD=100.0
DCA_PERIOD=1_day  # Options: "1_minute", "1_hour", "1_day"
DCA_DAILY_TIME_UTC=18:00  # Only used when DCA_PERIOD is set to "1_day"

# Report configuration
REPORT_TIMES_UTC=09:01,21:01  # Comma-separated list of times (HH:MM) in UTC to send reports
REPORT_LOOKBACK_HOURS=12  # Number of hours to look back for statistics in reports

# Portfolio settings (track your existing BTC alongside DCA purchases)
PORTFOLIO_INITIAL_BTC=0.0  # Amount of BTC you already hold
PORTFOLIO_INITIAL_AVG_PRICE=0.0  # Average purchase price of your existing BTC

# MongoDB configuration
MONGODB_URI=mongodb+srv://username:password@clustername.mongodb.net/dca_bot?retryWrites=true&w=majority

# Application modes
DRY_RUN=false  # Simulates trades without actually executing them on the exchange
RUN_IMMEDIATELY=false  # Executes a DCA purchase immediately on startup, not waiting for the next scheduled time

# Logging
LOG_LEVEL=INFO
